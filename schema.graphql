type Recipe {
  id: ID!
  title: String!
  category: String!
  ingredients: String!
  instructions: String!
  createdAt: String!
}

type Query {
  recipes(title: String, category: String, ingredients: String): [Recipe!]!
}
